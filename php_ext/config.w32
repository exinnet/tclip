// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("exclip", "for exclip support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("exclip", "enable exclip support", "no");

if (PHP_EXCLIP != "no") {
	EXTENSION("exclip", "exclip.c");
}

